*Closed record — see [README.md](README.md). Not spec.*

# Fixed problems — (empty)

Entries closed out of [PROBLEMS.md](../PROBLEMS.md), oldest first. **The heading
states the range this shard actually holds; it is derived and machine-checked.**

Each entry is `**P<n> — <title>**`, the defect as it was recorded, and the
commit that closed it. A docs defect closes with the docs edit; an
implementation-lag entry closes with the implementing commit. Because the fix
and its history entry land together, the hash is written in a small follow-up
commit.

Moving an entry here is what makes the register's `fixed` count go up — the
count is derived from these shards, so a fix that never gets moved is a fix the
headline does not believe in.

*(nothing fixed yet)*
